# @package __global__

# total samples obtained from MusicCaps = 5469
# (out of 5521 due to AudioSet corrupted samples)
datasource:
  max_sample_rate: 96000
  max_channels: 2

  train: /root/dataset/manifests/train
  valid: /root/dataset/manifests/valid
  evaluate: null
  generate: /root/dataset/manifests/valid
